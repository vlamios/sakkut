---
# ansible-playbook -i inventories/sandbox/hosts --vault-password-file ../vault.key applyRole.yaml --become --become-method=sudo --become-user=root
#
#
 - hosts: Hekke
   roles:
   - openvpn

# - hosts: RemoteStations
#   roles:
#   - common
